// 애플리케이션 서비스 / 유스케이스 모듈

dependencies {
	// 내부 모듈 의존성
	implementation project(':edukit-domain')
	
	// 필요한 의존성은 나중에 추가
}

// 라이브러리 JAR 생성
jar {
	archiveBaseName = 'edukit-application'
	archiveVersion = project.version
	enabled = true
} 
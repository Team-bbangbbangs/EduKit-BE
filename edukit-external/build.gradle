dependencies {
	// 내부 모듈 의존성
	implementation project(':edukit-global-utils')

}

// JAR 파일 설정 (라이브러리 모듈이므로 일반 JAR)
jar {
	archiveBaseName = 'edukit-external'
	archiveVersion = project.version
	enabled = true
} 

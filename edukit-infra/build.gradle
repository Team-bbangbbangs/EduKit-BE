// 인프라스트럭처 모듈 (DB, 외부 API 등)

dependencies {
	// 내부 모듈 의존성
	implementation project(':edukit-domain')
	implementation project(':edukit-application')
	
	// 필요한 의존성은 나중에 추가
}

// 라이브러리 JAR 생성
jar {
	archiveBaseName = 'edukit-infra'
	archiveVersion = project.version
	enabled = true
} 